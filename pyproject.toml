[project]
name = "splleing-analysis"
version = "0.1.0"
description = "Local system-wide keystroke capture for offline spelling analysis"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pynput>=1.8.1",
]
